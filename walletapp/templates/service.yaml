apiVersion: v1
kind: Service
metadata:
  name: {{ include "walletapp.fullname" . }}
  labels:
    {{- include "walletapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "walletapp.selectorLabels" . | nindent 4 }}
