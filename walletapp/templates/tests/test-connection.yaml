apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "walletapp.fullname" . }}-test-connection"
  labels:
    {{- include "walletapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "walletapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
